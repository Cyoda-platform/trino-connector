# README #

This module mimics the presto-server module, so that we can run presto from the IDE without actually checking out the
presto project.

It configures only the cyoda-presto plugin, so startup is quicker, too. :-)

